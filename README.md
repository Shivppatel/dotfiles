# dotfiles
symlink locations
```bash
~/.tmux.conf
~/.wezterm.lua
~/.confg/alacritty/
~/.confg/nvim/
~/.confg/sketchybar/
~/.confg/skhd/
~/.config/starship/
~/.confg/yabai/
```

Start commands
```bash
skdh --start-service
yabai --start-service
brew services start sketchybar
```

Restart commands
```bash
skhd --restart-service
yabai --restart-service
sketchybar --reload
```
